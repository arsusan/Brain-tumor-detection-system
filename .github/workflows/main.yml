name: NeuroScan AI CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          lfs: true # Ensures LFS pointers are handled

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install ML Dependencies
        # Note: Changed to use the backend folder as root
        run: pip install -r backend/requirements.txt

      - name: Run Backend Unit Tests (Pytest)
        run: pytest backend/tests/

  frontend-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: Install and Test React Components
        run: |
          cd frontend
          npm install
          npm test

  deploy:
    needs: [backend-tests, frontend-tests]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true

      - name: Push to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          # The key fix for your LFS "Missing Object" error
          git config lfs.allowincompletepush true

          # Force push the backend folder to Hugging Face
          # Since your HF Space expects the Dockerfile at the root, 
          # we push the contents of your 'backend' directory.
          git subtree push --prefix backend https://arsusan:$HF_TOKEN@huggingface.co/spaces/arsusan/brain-tumor-detection-api main
